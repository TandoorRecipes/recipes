{
  "name": "e2e_tests",
  "version": "1.0.0",
  "description": "ui tests for tandoor recipe app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:e2e": "cucumber-js --require cucumber.conf.js --require e2e/stepDefinitions/**/*.js --format @cucumber/pretty-formatter"
  },
  "author": "Jankari Tech",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^9.1.2",
    "@cucumber/pretty-formatter": "^1.0.0",
    "@playwright/test": "^1.34.3",
    "playwright": "^1.34.3"
  },
  "dependencies": {
    "dotenv": "^16.1.4"
  }
}
